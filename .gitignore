# Compiled binaries
/ente-public-galleries
/gallery-visibility

# Generated site
/out/

# Incremental sync cache. Derived data only, safe to delete; see internal/gallery.
/.cache/

# Real config, as distinct from the committed config.yaml.example. Holds your
# email and server details, so it stays out of git.
/config.yaml
